# Example Inputs

Place one example pair in the selected modality directory:

```text
<modality>/
├── image0.png
├── image1.png
└── homography.txt  # required only for demo mode 2
```

For mode 2, `homography.txt` must contain a 3x3 matrix mapping coordinates
from `image0` to `image1`.

